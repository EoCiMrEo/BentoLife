[package]
name = "bentolife_core"
version = "0.1.0"
edition = "2021"
description = "Pure BentoLife vault, import, snapshot, cache, and graph logic"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ts-rs = { version = "10", features = ["serde-compat"] }
