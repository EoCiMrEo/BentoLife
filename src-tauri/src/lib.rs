mod backend;

pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_dialog::init())
        .invoke_handler(tauri::generate_handler![
            backend::commands::get_default_vault_path,
            backend::commands::inspect_vault,
            backend::commands::create_default_vault,
            backend::commands::create_vault_at,
            backend::commands::repair_vault_structure,
            backend::commands::manage_markdown_document,
            backend::commands::rebuild_workspace_index,
            backend::commands::scan_workspace,
            backend::commands::read_dashboard_hub,
            backend::commands::pin_dashboard_entity,
            backend::commands::unpin_dashboard_entity,
            backend::commands::preview_workspace_recovery,
            backend::commands::recover_document_metadata,
            backend::commands::recover_layout_metadata,
            backend::commands::orphan_missing_document_metadata,
            backend::commands::restore_orphaned_document_metadata,
            backend::commands::repair_document_frontmatter_reference,
            backend::commands::load_layout_metadata,
            backend::commands::save_layout_metadata,
            backend::commands::list_notes,
            backend::commands::read_note,
            backend::commands::create_note,
            backend::commands::update_note,
            backend::commands::rename_note,
            backend::commands::save_markdown_asset,
            backend::commands::read_markdown_asset,
            backend::commands::list_core_modules,
            backend::commands::load_module_registry,
            backend::commands::set_module_enabled,
            backend::commands::load_widget_types,
            backend::commands::load_dashboard_widgets,
            backend::commands::create_dashboard_widget,
            backend::commands::update_dashboard_widget,
            backend::commands::remove_dashboard_widget,
            backend::commands::duplicate_dashboard_widget,
            backend::commands::move_dashboard_widget,
            backend::commands::resize_dashboard_widget,
            backend::commands::set_dashboard_widget_collapsed,
            backend::commands::reset_dashboard_widgets,
            backend::commands::compact_dashboard_widgets,
            backend::commands::load_workspace_ui_state,
            backend::commands::save_workspace_ui_state,
            backend::commands::list_todos,
            backend::commands::read_todo,
            backend::commands::create_todo,
            backend::commands::update_todo,
            backend::commands::rename_todo,
            backend::commands::read_contacts,
            backend::commands::create_contact,
            backend::commands::update_contact,
            backend::commands::read_habits,
            backend::commands::create_habit,
            backend::commands::update_habit,
            backend::commands::record_habit_checkin,
            backend::commands::validate_layout_import,
            backend::commands::import_layout_file,
            backend::commands::validate_widget_layout_import,
            backend::commands::import_widget_layout_file,
            backend::commands::export_widget_layout_file,
            backend::commands::validate_theme_import,
            backend::commands::import_theme_file,
            backend::commands::read_active_theme,
            backend::commands::preview_theme_tokens,
            backend::commands::apply_theme_tokens,
            backend::commands::rollback_theme,
            backend::commands::preview_folder_import,
            backend::commands::import_folder_into_vault,
            backend::commands::preview_vault_snapshot,
            backend::commands::create_vault_snapshot,
            backend::commands::preview_snapshot_restore,
            backend::commands::restore_vault_snapshot,
            backend::commands::stage_snapshot_for_import,
            backend::commands::list_staged_imports,
            backend::commands::preview_accept_import,
            backend::commands::accept_import_into_module,
            backend::commands::ignore_staged_import,
            backend::commands::bulk_accept_imports,
            backend::commands::rebuild_core_cache,
            backend::commands::preview_entity_upgrade,
            backend::commands::apply_entity_upgrade,
            backend::commands::read_navigator,
            backend::commands::rebuild_navigator,
            backend::commands::scan_and_rebuild_navigator,
            backend::commands::rebuild_search_index,
            backend::commands::search_entities,
            backend::commands::trash_managed_entity,
            backend::commands::restore_trashed_entity,
            backend::commands::archive_managed_entity,
            backend::commands::restore_archived_entity,
            backend::commands::list_trash_entries,
            backend::commands::list_archive_entries,
            backend::commands::restore_trash_entry,
            backend::commands::restore_archive_entry,
            backend::commands::delete_trash_entry_permanently,
            backend::commands::empty_trash,
        ])
        .run(tauri::generate_context!())
        .expect("error while running BentoLife");
}
